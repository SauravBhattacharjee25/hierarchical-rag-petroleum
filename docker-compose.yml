version: '3.8'

services:
  rag-system:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=AIzaSyBrFMhJzP-Tl_H5F_017JhKj5ySFPdWvVM
      - PYTHONUNBUFFERED=1
    volumes:
      - ./wells_complete_db.pkl:/app/wells_complete_db.pkl
      - ./uploads:/app/uploads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
